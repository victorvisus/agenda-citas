import { HORARIO } from '../utils/Validaciones.js';

const cont = HORARIO.length;

/**
 * este archivo tiene que montar el DOM de las citas, de momento sin tener en cuetna que haya citas o no.
 *
 */
const daySection = document.getElementById('appointments-day-container-id');
